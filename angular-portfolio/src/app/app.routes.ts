import { Routes } from '@angular/router';
import { TempPortfolioComponent } from './components/temp-portfolio/temp-portfolio.component';

export const routes: Routes = [
    { path: '', component: TempPortfolioComponent }
];
